import React from 'react';

const SearchBar = props => {
  return (
    <React.Fragment>
      <input 
        style={styles.input}
      />
    </React.Fragment>
  )
};

export default SearchBar;

const styles = {
};